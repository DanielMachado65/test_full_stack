<p id="notice"><%= notice %></p>

<h1>Ingressos</h1>

<table>
  <thead>
    <tr>
      <th>Preco</th>
      <th>Nome</th>
      <th>Taxa</th>
      <th>Data</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @ingressos.each do |ingresso| %>
      <tr>
        <td><%= ingresso.preco %></td>
        <td><%= ingresso.nome %></td>
        <td><%= ingresso.taxa %></td>
        <td><%= ingresso.data %></td>
        <td><%= link_to 'Show', ingresso %></td>
        <td><%= link_to 'Edit', edit_ingresso_path(ingresso) %></td>
        <td><%= link_to 'Destroy', ingresso, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Ingresso', new_ingresso_path %>
