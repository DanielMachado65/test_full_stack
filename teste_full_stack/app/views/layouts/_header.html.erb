<nav class="navbar navbar-expand-lg navbar-dark bg-dark navbar-social">
  <% if current_comprador %>
    <h3><%= link_to "Social Wave", home_path, :class => "title_header"%></h3>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarText">
      <ul class="navbar-nav mr-auto"></ul>

      <%= link_to 'Editar Perfil', edit_comprador_path(current_comprador.id), :class => "btn btn-outline-danger mr-2 title_btn" %>

      <%= button_to "Logout", destroy_comprador_session_path, :class => "btn btn-outline-danger title_btn", :method => :delete %>
    </div>
  <% else %>
    <%= link_to "Bem-Vindo", home_path, :class=>"title_header" %>
  <% end %>
</nav>
