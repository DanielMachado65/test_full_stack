<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <% if current_comprador %>
    <%= link_to "Home", home_path, :class=>"text-muted" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarText">
      <ul class="navbar-nav mr-auto"></ul>

      <%= link_to 'Edit', edit_comprador_path(current_comprador.id), :class => "btn btn-outline-danger mr-2" %>
      
      <%= button_to "Logout", destroy_comprador_session_path, :class => "btn btn-outline-danger", :method => :delete %>
    </div>
  <% else %>
    <%= link_to "Bem-Vindo", home_path, :class=>"text-muted" %>
  <% end %>
</nav>
